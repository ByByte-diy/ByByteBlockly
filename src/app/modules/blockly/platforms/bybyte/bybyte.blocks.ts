import { BlockDefinition } from "../../types/block.types";

/**
 * ByByte-specific blocks — add new files under platforms/bybyte/ and register here.
 * Use `.setBoards(['bybyte_nano', 'bybyte_mega'])` on each block definition.
 */
export const BYBYTE_BLOCKS: BlockDefinition[] = [];
